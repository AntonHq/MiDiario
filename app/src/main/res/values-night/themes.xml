<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.DiarioPersonal.Dark" parent="Base.Theme.DiarioPersonal">
        <!-- Customize your dark theme here -->
        <item name="colorPrimary">@color/btnEliminar</item>
        <item name="colorOnPrimary">@color/btnEditar</item>
        <item name="colorSecondary">@color/btnCerrarSesion</item>
        <item name="colorOnSecondary">@color/text</item>
        <item name="background">@color/background</item>
        <item name="color">@color/staticWhite</item>
        <item name="backgroundColor">@color/variableBack</item>
        <item name="textFillColor">@color/BlackToWhite</item>
        <item name="content">@color/colorBtnTercero</item>

        <!-- Other customizations for dark theme -->
    </style>
</resources>
